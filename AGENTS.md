# Agent Instructions

- Keep the repository layout stable and minimal.
- Store skills under `skills/<name>/SKILL.md`.
- Place shared guidance in `instructions/`.
- Use `scripts/` only when automation is needed.
- Add package/tooling files only when required for actual tasks.
